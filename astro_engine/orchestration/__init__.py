"""
Orchestration Layer — Semantic contracts and payload builders for future API/LLM integration.

This module defines STABLE INTERFACES between astro_engine symbolic cognition
and future production systems (Personality API, Timing API, Career API, OpenAI).

It does NOT call external APIs. It produces structured payloads only.
"""
